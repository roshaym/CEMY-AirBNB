<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <%= link_to root_path, class: "navbar-brand" do %>
      <%= image_tag asset_path("logo.png"), alt: "Logo", class: "app-logo img-fluid", style: "width: 80px; height: auto;" %>

    <% end %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar" aria-controls="mainNavbar" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="mainNavbar">
      <ul class="navbar-nav ms-auto align-items-center">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to new_property_path, class: "nav-link" do %>
              <i class="fa-solid fa-house-medical me-2"></i>Add Property
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to properties_path, class: "nav-link" do %>
              <i class="fa-solid fa-list me-2"></i>Browse Properties
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to root_path, class: "nav-link" do %>
              <i class="fa-solid fa-house me-2"></i>Dashboard
            <% end %>
          </li>
          <li class="nav-item dropdown">
            <a href="#" class="nav-link dropdown-toggle" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <% if current_user.avatar.attached? %>
                <%= image_tag current_user.avatar, class: "rounded-circle", style: "width: 32px; height: 32px; object-fit: cover;", alt: "Avatar" %>
              <% else %>
                <i class="fa-solid fa-circle-user fs-4"></i>
              <% end %>
            </a>
            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
              <li>
                <%= link_to edit_user_path(current_user), class: "dropdown-item" do %>
                  <i class="fa-solid fa-user-pen me-2"></i>Edit Profile
                <% end %>
              </li>
              <li>
                <%= link_to "#", class: "dropdown-item" do %>
                  <i class="fa-solid fa-gear me-2"></i>Settings
                <% end %>
              </li>
              <li><hr class="dropdown-divider"></li>
              <li>
                <%= link_to destroy_user_session_path, data: { turbo_method: :delete }, class: "dropdown-item" do %>
                  <i class="fa-solid fa-right-from-bracket me-2"></i>Logout
                <% end %>
              </li>
            </ul>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to new_user_session_path, class: "btn btn-outline-primary" do %>
              <i class="fa-solid fa-right-to-bracket me-2"></i>Login
            <% end %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
