<div class="container mt-5">
  <div class="card">
    <div class="card-header">
      <h1><%= @property.name %></h1>
    </div>
    <div class="card-body">
      <p><strong>Location:</strong> <%= @property.location %></p>
      <p><strong>Rating:</strong> <%= @property.rating %></p>

      

    </div>
  </div>
</div>

 <div class="text-center mt-4">
        <%= link_to 'Back to Listings', properties_path, class: 'btn btn-primary btn-lg shadow-sm rounded-pill px-5 py-2' %>
      </div>
